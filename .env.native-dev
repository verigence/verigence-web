# Capacitor DEV build endpoints.
# The native WebView does not share the Cloudflare browser origin, so Security calls
# use the deployed Web Worker gateway explicitly.
VITE_SECURITY_BASE_URL=https://verigence-web-dev.jbrconsulting-it.workers.dev

# Route Audit Core through the same Cloudflare DEV worker used by Web DEV.
# This avoids direct native-WebView calls to Railway, including CORS/origin mismatch.
VITE_AUDIT_CORE_BASE_URL=
VITE_AUDIT_CORE_PROXY_BASE_URL=https://verigence-web-dev.jbrconsulting-it.workers.dev/audit-core

# Set to the public Railway Attendance DEV service URL for native builds.
# Attendance remains optional and must never gate normal mobile Verigence work.
VITE_ATTENDANCE_BASE_URL=
